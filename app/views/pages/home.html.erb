<h1>Richer</h1>
<% if user_signed_in? %>
  <h2>Welcome back, <%= current_user.given_name %>!</h2>
<% end %>
<p>Finance tracker.</p>
<% if user_signed_in? %>
  <%= button_to 'Log Out', logout_path, method: :delete %>
<% else %>
  <%= link_to 'Log In', login_path %>
<% end %>
